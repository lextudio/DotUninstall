<UserControl
  x:Class="DotNetUninstall.Presentation.Controls.SingleBadge"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignHeight="24"
  d:DesignWidth="120">
  <Border x:Name="RootBorder" Padding="5,2" MinHeight="20" Background="{x:Bind Background, Mode=OneWay}" CornerRadius="4" VerticalAlignment="Center">
    <ContentPresenter
      x:Name="ContentHost"
      Content="{x:Bind Content, Mode=OneWay}"
      VerticalAlignment="Center"
      HorizontalAlignment="Stretch" />
  </Border>
</UserControl>