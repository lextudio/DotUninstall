<Page
  x:Class="DotNetUninstall.Presentation.MainPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:controls="using:DotNetUninstall.Presentation.Controls"
  xmlns:conv="using:DotNetUninstall.Presentation.Converters"
  xmlns:sys="using:System"
  x:Name="RootPage"
  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
  RequestedTheme="{Binding RequestedElementTheme}">
  <Page.Resources>
    <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    <conv:BoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" Invert="True" />
    <conv:StringEqualsToVisibilityConverter x:Key="StringEqualsToVisibilityConverter" />
    <conv:LifecycleStateToBrushConverter x:Key="LifecycleStateToBrushConverter" />
    <conv:ChannelDisplayConverter x:Key="ChannelDisplayConverter" />
    <conv:SecurityStateYesNoConverter x:Key="SecurityStateYesNoConverter" />
    <Style x:Key="BannerActionButtonStyle" TargetType="Button">
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="CornerRadius" Value="6" />
      <Setter Property="Foreground" Value="{ThemeResource Brush.Banner.Action.Primary.Foreground}" />
      <Setter Property="Background" Value="{ThemeResource Brush.Banner.Action.Primary.Background}" />
      <Setter Property="BorderBrush" Value="{ThemeResource Brush.Banner.Action.Primary.Border}" />
    </Style>
    <Style x:Key="BannerSecondaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource BannerActionButtonStyle}">
      <Setter Property="Foreground" Value="{ThemeResource Brush.Banner.Action.Secondary.Foreground}" />
      <Setter Property="Background" Value="{ThemeResource Brush.Banner.Action.Secondary.Background}" />
      <Setter Property="BorderBrush" Value="{ThemeResource Brush.Banner.Action.Secondary.Border}" />
    </Style>
    <DataTemplate x:Key="HeaderTemplate">
      <StackPanel
        Margin="8,0,8,12"
        Padding="0"
        CornerRadius="6">
        <ToggleButton
          x:Name="GroupToggle"
          Padding="0"
          HorizontalAlignment="Stretch"
          Background="Transparent"
          BorderThickness="0"
          IsChecked="True">
          <StackPanel
            VerticalAlignment="Center"
            Orientation="Horizontal"
            Spacing="8">
            <TextBlock
              FontSize="{StaticResource FontSize.ChannelTitle}"
              FontWeight="SemiBold"
              Text=".NET" />
            <TextBlock
              FontSize="{StaticResource FontSize.ChannelTitle}"
              FontWeight="Bold"
              Text="{Binding Channel, Converter={StaticResource ChannelDisplayConverter}}" />
          </StackPanel>
        </ToggleButton>
        <Grid
          Margin="0,4,4,0"
          Background="Transparent"
          Visibility="{Binding IsChecked, ElementName=GroupToggle, Converter={StaticResource BoolToVisibilityConverter}}">
          <StackPanel>
            <!--  Badge row (two-segment style: label | value)  -->
            <StackPanel
              Margin="0,6,0,4"
              Orientation="Horizontal"
              Spacing="8">
              <!--  Release Type (e.g., LTS | 8)  -->
              <controls:TwoPartBadge
                InfoGlyphForeground="{ThemeResource Badge.Release.Label}"
                InfoTag="{Binding ReleaseTypeInfoUrl}"
                InfoToolTip="Learn more"
                Label="Type"
                LabelBackground="{ThemeResource Badge.Release.Label}"
                ValueBackground="{ThemeResource Badge.Release.Value}"
                Visibility="{Binding ReleaseType, Converter={StaticResource NullToVisibilityConverter}}"
                Value="{Binding ReleaseType}" />
              <!--  Support Phase (Phase | value)  -->
              <controls:TwoPartBadge
                InfoGlyphForeground="{ThemeResource Badge.Support.Value}"
                InfoTag="{Binding SupportPhaseInfoUrl}"
                InfoToolTip="Learn more"
                Label="Support"
                LabelBackground="{ThemeResource Badge.Support.Label}"
                ValueBackground="{ThemeResource Badge.Support.Value}"
                Visibility="{Binding SupportPhase, Converter={StaticResource NullToVisibilityConverter}}"
                Value="{Binding SupportPhase}" />
              <!--  EOL badge (EOL | date)  -->
              <controls:TwoPartBadge
                InfoGlyphForeground="{ThemeResource Badge.Eol.Value}"
                InfoTag="{Binding EolInfoUrl}"
                InfoToolTip="Learn more"
                Label="EOL"
                LabelBackground="{ThemeResource Badge.Eol.Label}"
                ToolTipService.ToolTip="{Binding EolDisplay}"
                ValueBackground="{ThemeResource Badge.Eol.Value}"
                Visibility="{Binding EolDateValue, Converter={StaticResource NullToVisibilityConverter}}"
                Value="{Binding EolDateValue}" />
              <!--  MAUI EOL badge (MAUI EOL | date)  -->
              <controls:TwoPartBadge
                InfoGlyphForeground="{ThemeResource Badge.MauiEol.Value}"
                InfoTag="{Binding MauiEolInfoUrl}"
                InfoToolTip="Learn more"
                Label="MAUI EOL"
                LabelBackground="{ThemeResource Badge.MauiEol.Label}"
                ToolTipService.ToolTip="MAUI End of life"
                ValueBackground="{ThemeResource Badge.MauiEol.Value}"
                Visibility="{Binding MauiEolDateValue, Converter={StaticResource NullToVisibilityConverter}}"
                Value="{Binding MauiEolDateValue}" />
              <!--  Latest missing (normal)  -->
              <controls:TwoPartBadge
                InfoGlyphForeground="{ThemeResource Badge.Latest.Value}"
                InfoTag="{Binding ChannelDownloadUrl}"
                InfoToolTip="Download"
                Label="Latest"
                LabelBackground="{ThemeResource Badge.Latest.Label}"
                ValueBackground="{ThemeResource Badge.Latest.Value}"
                Visibility="{Binding ShowLatestRelevantMissingNormal, Converter={StaticResource BoolToVisibilityConverter}}"
                Value="{Binding LatestRelevantVersion}" />
              <!--  Latest missing (security)  -->
              <controls:TwoPartBadge
                InfoGlyphForeground="{StaticResource Brush.Security.Alert}"
                InfoTag="{Binding ChannelDownloadUrl}"
                InfoToolTip="Download"
                Label="Latest"
                LabelBackground="{StaticResource Brush.Security.Alert}"
                ValueBackground="{StaticResource Brush.Security.Warning}"
                Visibility="{Binding ShowLatestRelevantMissingSecurity, Converter={StaticResource BoolToVisibilityConverter}}"
                Value="{Binding LatestRelevantVersion}" />
            </StackPanel>
            <ListView
              ItemTemplate="{StaticResource InstallEntryTemplate}"
              ItemsSource="{Binding Items}"
              SelectionMode="None" />
          </StackPanel>
        </Grid>
      </StackPanel>
    </DataTemplate>
    <DataTemplate x:Key="InstallEntryTemplate">
      <Grid
        MinHeight="48"
        Padding="4"
        ColumnDefinitions="*,Auto"
        ToolTipService.ToolTip="{Binding Reason}">
        <StackPanel
          Margin="4"
          Orientation="Vertical"
          Spacing="4">
          <StackPanel
            VerticalAlignment="Center"
            Orientation="Horizontal"
            Spacing="6">
            <TextBlock
              FontSize="{StaticResource FontSize.VersionPrimary}"
              FontWeight="Bold"
              Text="{Binding Version}" />
            <TextBlock
              MaxWidth="360"
              VerticalAlignment="Center"
              FontSize="{StaticResource FontSize.Badge}"
              Opacity="0.65"
              Text="{Binding DisplayName}"
              TextTrimming="CharacterEllipsis" />
          </StackPanel>
          <StackPanel
            VerticalAlignment="Center"
            Orientation="Horizontal"
            Spacing="6">
            <!--  Release Date badge: Release|yyyy-MM-dd with notes link  -->
            <controls:TwoPartBadge
              InfoGlyphForeground="{ThemeResource Badge.ReleaseDate.Label}"
              InfoTag="{Binding ReleaseNotesUrl}"
              InfoToolTip="Open release notes"
              Label="Release"
              LabelBackground="{ThemeResource Badge.ReleaseDate.Label}"
              ToolTipService.ToolTip="View release notes"
              ValueBackground="{ThemeResource Badge.ReleaseDate.Value}"
              Visibility="{Binding ReleaseDate, Converter={StaticResource NullToVisibilityConverter}}"
              Value="{Binding ReleaseDateValue}" />
            <!--  Stage badge: Stage|GA or Stage|Preview N / Stage|RC N  -->
            <controls:TwoPartBadge
              Label="Stage"
              LabelBackground="{ThemeResource Badge.Stage.Label}"
              ToolTipService.ToolTip="Release stage"
              ValueBackground="{ThemeResource Badge.Stage.Value}"
              Visibility="{Binding StageDisplay, Converter={StaticResource NullToVisibilityConverter}}"
              Value="{Binding StageDisplay}" />
            <!--  Simplified secure status badge: Secure|Yes or Secure|No  -->
            <controls:TwoPartBadge
              Label="Secure"
              LabelBackground="{ThemeResource Badge.Security.Label}"
              ToolTipService.ToolTip="{Binding SecurityTooltip}"
              Visibility="{Binding ElementName=RootPage, Path=DataContext.IsMetadataEnabledInSession, Converter={StaticResource BoolToVisibilityConverter}}"
              ValueBackground="{ThemeResource Badge.Security.Value}"
              Value="{Binding SecurityStatus, Converter={StaticResource SecurityStateYesNoConverter}}" />
            <!--  Out of support badge  -->
            <controls:TwoPartBadge
              Label="EOL"
              LabelBackground="{ThemeResource Badge.Eol.Label}"
              ToolTipService.ToolTip="Out of support"
              ValueBackground="{ThemeResource Badge.Eol.Value}"
              Visibility="{Binding IsOutOfSupport, Converter={StaticResource BoolToVisibilityConverter}}"
              Value="Build" />
            <!--  Architecture badge  -->
            <controls:TwoPartBadge
              Label="Arch"
              LabelBackground="{ThemeResource Badge.Arch.Label}"
              ValueBackground="{ThemeResource Badge.Arch.Value}"
              Value="{Binding Architecture}" />
            <!--  SubType badge  -->
            <controls:TwoPartBadge
              BadgeFontSize="11"
              Label="Type"
              LabelBackground="{ThemeResource Badge.SubType.Label}"
              ValueBackground="{ThemeResource Badge.SubType.Value}"
              Visibility="{Binding SubType, Converter={StaticResource NullToVisibilityConverter}}"
              Value="{Binding SubType}" />
            <!--  Reason badge (single segment)  -->
            <controls:SingleBadge Background="{ThemeResource Badge.Reason.Background}" Visibility="{Binding Reason, Converter={StaticResource NullToVisibilityConverter}}">
              <TextBlock
                MaxWidth="260"
                VerticalAlignment="Center"
                FontSize="{StaticResource FontSize.Badge}"
                Foreground="White"
                Text="{Binding Reason}"
                TextTrimming="CharacterEllipsis" />
            </controls:SingleBadge>
            <!--  Removable badge  -->
            <controls:TwoPartBadge
              Label="State"
              LabelBackground="{ThemeResource Badge.State.Label}"
              ValueBackground="{ThemeResource Badge.State.Value}"
              Visibility="{Binding CanUninstall, Converter={StaticResource BoolToVisibilityConverter}}"
              Value="Removable" />
          </StackPanel>
        </StackPanel>
        <Button
          Grid.Column="1"
          Margin="4"
          Command="{Binding ElementName=RootPage, Path=DataContext.UninstallCommand}"
          CommandParameter="{Binding}"
          Content="Uninstall"
          IsEnabled="{Binding CanUninstall}"
          Loaded="Button_Loaded" />
      </Grid>
    </DataTemplate>
  </Page.Resources>
  <Grid RowDefinitions="Auto,*,Auto">
    <StackPanel
      Grid.Row="0"
      Margin="0,4,0,4"
      Spacing="4">
      <!--  Snapshot / cache status banner  -->
      <Border
        Margin="8,8,8,0"
        Padding="8"
        Background="{ThemeResource Brush.Banner.Snapshot.Background}"
        BorderBrush="{ThemeResource Brush.Banner.Snapshot.Border}"
        BorderThickness="1"
        CornerRadius="6"
        Visibility="{Binding IsUsingSnapshot, Converter={StaticResource BoolToVisibilityConverter}}">
        <StackPanel
          VerticalAlignment="Center"
          Orientation="Horizontal"
          Spacing="8">
          <TextBlock
            FontSize="12"
            Foreground="{ThemeResource Brush.Banner.Snapshot.Foreground}"
            Text="Using embedded metadata snapshot (offline)." />
        </StackPanel>
      </Border>
      <Border
        Margin="8,0,8,0"
        Padding="8"
        Background="{ThemeResource Brush.Banner.Cached.Background}"
        BorderBrush="{ThemeResource Brush.Banner.Cached.Border}"
        BorderThickness="1"
        CornerRadius="6"
        Visibility="{Binding IsUsingCachedLive, Converter={StaticResource BoolToVisibilityConverter}}">
        <StackPanel
          VerticalAlignment="Center"
          Orientation="Horizontal"
          Spacing="8">
          <TextBlock
            FontSize="12"
            Foreground="{ThemeResource Brush.Banner.Cached.Foreground}"
            Text="On cached live metadata." />
        </StackPanel>
      </Border>
      <!--  Update available banner  -->
      <Border
        Margin="8,0,8,0"
        Padding="12"
        Background="{ThemeResource Brush.Banner.Update.Background}"
        BorderBrush="{ThemeResource Brush.Banner.Update.Border}"
        BorderThickness="1"
        CornerRadius="6"
        Visibility="{Binding HasUpdate, Converter={StaticResource BoolToVisibilityConverter}}">
        <StackPanel
          VerticalAlignment="Center"
          Orientation="Horizontal"
          Spacing="12">
          <TextBlock
            MaxWidth="640"
            FontSize="12"
            Foreground="{ThemeResource Brush.Banner.Update.Foreground}"
            TextWrapping="WrapWholeWords">
            <Run Text="Update available: " />
            <Run Text="{Binding UpdateMessage}" />
          </TextBlock>
          <Button
            Padding="8,4"
            Click="OnOpenReleasePage"
            Style="{StaticResource BannerActionButtonStyle}"
            Content="Open Release Page" />
        </StackPanel>
      </Border>
      <!--  Elevation / sudo warning banner (macOS)  -->
      <Border
        Margin="8,0,8,0"
        Padding="12"
        Background="{ThemeResource Brush.Banner.Elevation.Background}"
        BorderBrush="{ThemeResource Brush.Banner.Elevation.Border}"
        BorderThickness="1"
        CornerRadius="6"
        Visibility="{Binding ShowElevationWarning, Converter={StaticResource BoolToVisibilityConverter}}">
        <StackPanel
          VerticalAlignment="Center"
          Orientation="Horizontal"
          Spacing="12">
          <TextBlock
            MaxWidth="640"
            FontSize="12"
            Foreground="{ThemeResource Brush.Banner.Elevation.Foreground}"
            TextWrapping="WrapWholeWords">
            <Run Text="Running with elevated privileges (root). " />
            <Run Text="Original user: " />
            <Run Text="{Binding OriginalUser}" />
            <Run Text=". Uninstalls may affect the entire system. Proceed with caution." />
          </TextBlock>
          <Button
            Padding="8,4"
            VerticalAlignment="Center"
            Style="{StaticResource BannerSecondaryActionButtonStyle}"
            Command="{Binding DismissElevationWarningCommand}"
            Content="Dismiss" />
        </StackPanel>
      </Border>
      <!--  Non-elevated offer banner (macOS)  -->
      <Border
        Margin="8,0,8,0"
        Padding="12"
        Background="{ThemeResource Brush.Banner.NonElevated.Background}"
        BorderBrush="{ThemeResource Brush.Banner.NonElevated.Border}"
        BorderThickness="1"
        CornerRadius="6"
        Visibility="{Binding ShowElevationOffer, Converter={StaticResource BoolToVisibilityConverter}}">
        <StackPanel
          VerticalAlignment="Center"
          Orientation="Horizontal"
          Spacing="12">
          <TextBlock
            MaxWidth="640"
            FontSize="12"
            Foreground="{ThemeResource Brush.Banner.NonElevated.Foreground}"
            TextWrapping="WrapWholeWords">
            <Run Text="Some uninstall operations may require administrator rights. " />
            <Run Text="You can relaunch this app with elevated privileges." />
          </TextBlock>
          <StackPanel
            VerticalAlignment="Center"
            Orientation="Horizontal"
            Spacing="8">
            <Button
              Padding="10,4"
              Style="{StaticResource BannerActionButtonStyle}"
              Command="{Binding ElevateCommand}"
              Content="Relaunch as Administrator" />
            <Button
              Padding="8,4"
              Style="{StaticResource BannerSecondaryActionButtonStyle}"
              Command="{Binding DismissElevationOfferCommand}"
              Content="Dismiss" />
          </StackPanel>
        </StackPanel>
      </Border>
    </StackPanel>
    <Pivot x:Name="MainPivot" Grid.Row="1">
      <PivotItem Header="Runtimes">
        <ScrollViewer>
          <StackPanel>
            <TextBlock
              Margin="12,0,12,8"
              FontSize="12"
              Opacity="0.7"><Run Text="Count: " /><Run Text="{Binding RuntimeCount}" /></TextBlock>
            <ItemsControl ItemTemplate="{StaticResource HeaderTemplate}" ItemsSource="{Binding GroupedRuntimeItems}" />
          </StackPanel>
        </ScrollViewer>
      </PivotItem>
      <PivotItem Header="SDKs">
        <ScrollViewer>
          <StackPanel>
            <TextBlock
              Margin="12,0,12,8"
              FontSize="12"
              Opacity="0.7"><Run Text="Count: " /><Run Text="{Binding SdkCount}" /></TextBlock>
            <ItemsControl ItemTemplate="{StaticResource HeaderTemplate}" ItemsSource="{Binding GroupedSdkItems}" />
          </StackPanel>
        </ScrollViewer>
      </PivotItem>
      <PivotItem Header="Settings">
        <ScrollViewer>
          <StackPanel Padding="12" Spacing="16">
            <TextBlock
              FontSize="14"
              FontWeight="Bold"
              Text="Application" />
            <StackPanel
              VerticalAlignment="Center"
              Orientation="Horizontal"
              Spacing="8">
              <TextBlock FontWeight="SemiBold" Text="Version:" />
              <TextBlock Text="{Binding AppVersion}" />
            </StackPanel>
            <ToggleSwitch
              Header="Use .NET metadata extras"
              IsOn="{Binding ShowDotNetMetadata, Mode=TwoWay}"
              OffContent="Off (dotnet-core-uninstall parity)"
              OnContent="On (show metadata-based details)" />
            <StackPanel Spacing="6">
              <TextBlock FontWeight="SemiBold" Text="Theme" />
              <ComboBox
                Width="180"
                ItemsSource="{Binding ThemeModes}"
                SelectedItem="{Binding ThemeMode, Mode=TwoWay}" />
              <TextBlock
                FontSize="12"
                Opacity="0.6"
                Text="Theme changes apply immediately." />
            </StackPanel>
            <TextBlock
              FontSize="12"
              Opacity="0.6"
              Text="Changes to this setting take effect after restarting the app." />
            <TextBlock
              FontSize="12"
              Foreground="{ThemeResource Brush.Banner.Update.Foreground}"
              Text="Restart required to apply metadata mode changes."
              Visibility="{Binding MetadataSettingChangedRequiresRestart, Converter={StaticResource BoolToVisibilityConverter}}" />
            <Button
              Padding="12,6"
              Command="{Binding CheckUpdateNowCommand}"
              Content="Check for Updates Now" />
            <Button
              Padding="12,6"
              Command="{Binding ClearCacheCommand}"
              Content="Clear Metadata Cache" />
            <TextBlock
              FontSize="12"
              Text="{Binding UpdateMessage}"
              TextWrapping="WrapWholeWords" />
            <TextBlock
              FontSize="12"
              Opacity="0.6"
              Text="Manual check bypasses the once-per-day automatic delay." />
          </StackPanel>
        </ScrollViewer>
      </PivotItem>
    </Pivot>
  </Grid>
</Page>
